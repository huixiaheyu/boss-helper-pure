# 下一步计划

## 待办

- [ ] 高德地图公共交通时间接入
  - 调研结论：高德支持公共交通路径规划，接口 `https://restapi.amap.com/v3/direction/transit/integrated`（返回多套换乘方案，每条含总耗时 duration）
  - 现有 `/v3/distance` 只支持直线/驾车/步行，不支持公交，需新增接口调用
  - 待接入（涉及 6 文件）：amap.ts 新增 amapTransit()、types.ts 增加 transitDuration 字段、info.ts 默认值+data-help、Address.vue 配置 UI、handles.ts 过滤任务、index.ts 配置项注册
  - 注意点：公交接口较重、每职位调用会增加延迟；需要城市(city)参数（需从起点地址反查或手动配置）
- [ ] 当选定日志 tab 时，投递刷新日志整个页面跟随到顶部（重新设计滚动跟随逻辑）
- [ ] 数据本地存储、日志分析、页面分析（方便调试）

## 已完成

- [X] 列方向标签和编辑框对齐（Address.vue 统一 label 宽度 w-28）
- [X] 更明显易懂的 label：0km、0分钟 的表示含义（info.ts data-help 明确"超过则跳过，0 表示不限制"，并补上 Address.vue 的 title 悬停提示）
- [X] 6 个过滤开关两端对齐（index.ts 容器加 justify-between 横向均匀排列）
- [X] 工作内容筛选写多了放不下（根因：UInputTags duplicate:false 去重导致重复标签静默拒绝，已加"已存在"提示）
- [X] 日志中的消息偏左偏右（Logs.vue 标题列固定宽度 size:180）
- [X] 已达到今日投递上限（120次）在日志显示（useApplying 投递上限写日志）
